<?xml version="1.0" encoding="UTF-8"?>
<form xmlns="http://jelix.org/ns/forms/1.1">

    <hidden ref="pchg_login" />
    <hidden ref="pchg_key" />

    <secret ref="pchg_password"  required="true" minlength="8" maxlength="120">
       <label locale="auth.form.change.password.label"/>
        <help locale="auth.form.password.help"/>
        <confirm locale="auth.form.password.confirm" />
    </secret>

   <submit ref="pchg_submit">
       <label locale="auth.form.change.submit"/>
   </submit>

</form>